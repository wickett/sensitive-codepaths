name: Check for changes in specified codepaths

on:
  push:
    branches:
      - '**'
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  check_changes:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Install yq
        run: |
          sudo snap install yq

      - name: Check for changes in specified codepaths
        id: check_changes
        run: | 
          # Read codepaths from sensitive-codepaths.yml
          codepaths=$(yq e '.codepaths' sensitive-codepaths.yml)
          changes_detected=false
          impacted_codepaths_count=0

          for codepath in $codepaths; do
            git diff --quiet --name-only ${{ github.event.before }} ${{ github.event.after }} -- "$codepath"
            if [ $? -eq 0 ]; then
              changes_detected=true
              impacted_codepaths_count=$((impacted_codepaths_count + 1))
            fi
          done

          pr_comment=""

          if [ "$changes_detected" == "true" ]; then
            pr_comment+="🚨 **$impacted_codepaths_count sensitive codepaths impacted!** 🚨\n\nChanges detected in specified codepaths. Please review carefully! 💥"
          else
            pr_comment+="🎉 **No sensitive codepaths impacted!** 🎉\n\nNo changes detected in specified codepaths. Good to go! 🚀"
          fi

          echo "::set-output name=pr_comment::$pr_comment"

      - name: Post result
        run: |
          pr_comment="${{ steps.check_changes.outputs.pr_comment }}"
          commit_sha="${{ steps.check_changes.outputs.commit_sha }}"
          
          if [ "$GITHUB_EVENT_NAME" == "pull_request" ]; then
            echo "$pr_comment" | gh pr comment --repo "$GITHUB_REPOSITORY" --body-file=-
          elif [ "$GITHUB_EVENT_NAME" == "push" ]; then
            json_payload=$(jq -n --arg body "$pr_comment" '{body: $body}')
            echo "$json_payload" | gh api "repos/$GITHUB_REPOSITORY/commits/$commit_sha/comments" --input=-
          fi

        env:
          GITHUB_TOKEN: ${{ secrets.MY_GITHUB_PAT }}